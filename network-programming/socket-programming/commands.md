```bash
sudo apt update

sudo apt install gcc telnet forkstat
```

```bash
sysctl net.core.somaxconn

cat /proc/sys/net/core/somaxconn
```

```bash
while true; do ./client; done;
```

```bash
while true; do ./client & done;
```
